[package]
name = "blackjack-protocol"
description = "The frontend/backend seam for Casino Blackjack: the Backend trait and wire types"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
blackjack-core = { path = "../blackjack-core" }
serde = { workspace = true }
# The shared session arc (SessionArc) seeds and drives TableLife<ChaCha8Rng>
# and randomizes the buy-in; all entropy is injected by the caller.
rand = { workspace = true }
rand_chacha = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
